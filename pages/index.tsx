//import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'
//import Link from 'next/link'
import styles from '../styles/Home.module.scss'
import {useRouter} from "next/router";

function Home() {
    const router = useRouter()
    const id = router.query
    const submitSearch = async (event:any) => {

    }
  return (
    <div className={styles.container}>
      <Head>
        <title>ultimEngine</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      {/*<header className={styles.header}>*/}
      {/*    <a href='help'>HELP</a>*/}
      {/*</header>*/}
        <main className={styles.main}>
            <div className={styles.main_div}>
                {/*<Link href="/?search=xda">*/}
                {/*    <a>Just a test</a>*/}
                {/*</Link>*/}
                <p>You searched for: {id.search}</p>
                <span className={styles.logo}>
                    <Image src="/logo-t.svg" alt="Out logo" width={144} height={32} />
                </span>
                <form className={styles.form} onSubmit={submitSearch}>
                    <input id="search" name="search" placeholder="Search..." aria-required={true}/>
                    <button type="submit">Search</button>
                </form>
            </div>
      </main>

    </div>
  )
}

// export async function getStaticProps(){
//
// }


export default Home
